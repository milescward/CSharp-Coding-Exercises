using System.Collections.Generic;
using System.Text.RegularExpressions;

namespace CSharp_Practice
{
    public class lcLongPressedName
    {
        /*Your friend is typing his name into a keyboard.  
        Sometimes, when typing a character c, the key 
        might get long pressed, and the character will 
        be typed 1 or more times.

        You examine the typed characters of the keyboard.  
        Return True if it is possible that it was your 
        friends name, with some characters (possibly 
        none) being long pressed. */

        
        public bool LongPressedNameCheck(string name, string typed)
        {
            return false;
        }
    }
    class Group {
    string key;
    List<int> count;
    Group(string k, List<int> c) {
        key = k;
        count = c;
    }
}
}